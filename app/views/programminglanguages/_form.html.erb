<%= form_with(model: programminglanguage, local: true) do |form| %>
  <% if programminglanguage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(programminglanguage.errors.count, "error") %> prohibited this programminglanguage from being saved:</h2>

      <ul>
        <% programminglanguage.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
